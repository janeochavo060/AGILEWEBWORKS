import React from "react";
import SingleImageText from "./SingleImageText";

export default function SectionedImageTextBlock({ slice }) {
  return (
    <div>
      <SingleImageText slice={slice} />
    </div>
  );
}
